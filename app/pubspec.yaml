name: time_journal
description: 时间管理手账 - 提升执行力、养成专注、规范作息
publish_to: 'none'
version: 1.0.2+3

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^17.3.0
  drift: ^2.34.0
  drift_flutter: ^0.3.0
  path_provider: ^2.1.6
  intl: ^0.20.2
  uuid: ^4.5.3
  flutter_local_notifications: ^22.0.1
  just_audio: ^0.10.5
  audio_session: ^0.2.3
  wakelock_plus: ^1.6.1
  shared_preferences: ^2.5.5
  timezone: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.0
  build_runner: ^2.15.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
