# v0.1 发布结论

| 项 | 内容 |
|----|------|
| 结论 | **可发（逻辑门禁通过）** |
| 日期 | 2026-07-09 |
| 分支 | `p0/journal-compare` |
| Commit | `835e6bf` |
| 验收依据 | `.memory/ACCEPTANCE_CHECKLIST.md` 核心 1–6 全绿；#9 自动化测试旁证 |
| 验证 | `flutter analyze` No issues；`flutter test` 150/150 |

## 产品边界（v0.1）

- **定位：** 低压力时间手账；核心是计划 vs 实际的温和对照。
- **包含：** P0–P2（今日对照、专注写回、`linkedPlanId`、时段高亮/空档、补记入口、滚轮选时、睡眠跨午夜逻辑、周报基础、导出）。
- **不包含（冻结 / 非 v0.1）：** P3 动效、更多白噪音、复杂拖拽安排、大 UI 重构、商店上架物料（若需另开）。

## 交付物建议

1. **APK：** 主路径 CI arm64 release → `fetch_arm64_apk_from_ci` → outbox  
   产物名：`time-journal-arm64-{sha}-{时间}.apk`
2. **变更摘要：** 见下方
3. **已知限制：** 见验收清单「已知限制」

## 变更摘要（相对 MVP 收口）

- 今日对照：按计划完成 / 实际有变 / 空档「当前没有计划」
- 专注完成写回：关联计划时直接「一致」；`linkedPlanId` 匹配与防 detach
- 待办：新建与安排/专注；**不做 UI 拖拽排序**（有意）
- 睡眠：跨午夜记录闭合与展示（逻辑已测）
- 交付：CI→outbox 主路径；UI 迭代 Web 预览优先

## 发布后建议（非 blocker）

1. 用户装机一次：冷启动 + 覆盖安装（清单 #1）
2. 可选：真实跨夜睡眠人肉（#9）
3. 需要商店/对外分发时再写完整 release notes 与版本号策略

## 本轮明确不做

- 不改 schema、不修非 blocker、不扩 P3、不纳入未提交 UI 审美 diff（已 revert）
